import { GetObsDataPipe } from './get-obs-data.pipe';

describe('GetObsDataPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObsDataPipe();
    expect(pipe).toBeTruthy();
  });
});
